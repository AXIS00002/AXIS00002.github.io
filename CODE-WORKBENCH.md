# 楽天 code 工作台

1. 输入 API 凭据、店铺代码或商品 URL、关键词获取商品。也可以在「商品资料与 code 编辑」直接粘贴商品 URL。
2. 导入原「コード設定」XLSX，选择工作表。文件在浏览器本机读取，不上传到仓库或服务。
3. 在商品列表中选择一件商品，选择对应的原表记录，点击「填入所选原表记录」。相同 URL 的多个商品番号会分别列出。
4. 核对并编辑型番、内部 code、順番、対照表、個数。未匹配的商品需手动填写，页面不按商品标题猜测这些内容。
5. 点击「复制三行 code」，粘贴到 Excel A 列。需要为同一个 URL 制作多个商品番号时，逐个选择、复制粘贴。

## 三行格式

| 行 | A | B | C | D | E 列起 |
| --- | --- | --- | --- | --- | --- |
| 1 | URL 末尾 ID | 店铺商品番号 | 型番 | 順番 | 颜色／顺序原文 |
| 2 | 分类标记 | 空白 | 空白 | 対照表 | 型号／选项原文 |
| 3 | 空白 | 内部 code | 空白 | 個数 | 数量，或原规则要求的空白 |

复制不包含表头，保留中间空列。0106 等前导零 ID 以 Excel 文本前缀复制。顔料与染料沿用原表内容，不互相替换。工作台每个 URL 暂存一份当前编辑内容，「复制所有已编辑 code」复制这些当前草稿。草稿仅保留在当前页面，刷新前请先复制。

## 页面详情

点击页面上的安装链接，在 Tampermonkey 安装 `rakuten-details.user.js`，刷新网页，再点击「获取所选商品 PC／手机版详情」。采集由按钮触发，匿名请求，不携带日常乐天 Cookie。

PC 与 `?force-site=ipn` 两次请求分别保留结果和失败提示。显示静态页面原文、表格和 HTML select 选项，供人工核对。页面通过 JavaScript 动态加载的选项可能获取不到。缺失字段需打开商品页核对，不能据此认定商品没有该选项。

公开 API 的商品说明和商品页详情分开显示。内部 code 来自原表或人工填写。未提供真实 API 凭据时无法验证实时 API 调用。

原有批量检查器的自动消息发送已关闭，获取商品后直接更新工作台。
